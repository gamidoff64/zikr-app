# Виртуальное окружение Python — сотни МБ, у каждого своё
.venv/

# Buildozer кэш и готовые APK — гигабайты, создаются при сборке
.buildozer/
bin/

# Python кэш — генерируется автоматически
__pycache__/
*.pyc
*.pyo

# Сохранённые данные пользователя — у каждого свои
zikr.json

# macOS мусор
.DS_Store
